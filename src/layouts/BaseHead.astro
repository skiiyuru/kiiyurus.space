---
import Posthog from '@/components/Posthog.astro'

import { baseData } from '@/data/siteData'

import { SEO } from 'astro-seo'
import { ClientRouter } from 'astro:transitions'

type Props = {
  title?: string
  description?: string
  image?: string
}

const { title = baseData.title, description = baseData.description, image = baseData.image.src }
  = Astro.props
---

<head>
  <meta charset="utf-8" />
  <link rel="icon" type="image/svg+xml" href="/memory-cube.svg" />
  <link rel="sitemap" href="/sitemap-index.xml" />
  <link
    rel="alternate"
    type="application/rss+xml"
    title={baseData.title}
    href={`${Astro.site}rss.xml`}
  />
  <meta name="viewport" content="width=device-width" />
  <meta name="generator" content={Astro.generator} />
  <SEO
    title={title}
    description={description}
    openGraph={{
    basic: {
      title,
      type: 'website',
      image,
    },
  }}
    twitter={{
    creator: '@snkiiyuru',
  }}
  />
  <!-- <meta http-equiv="Content-Security-Policy" content="
    default-src 'self';
    script-src 'self' 'unsafe-inline' 'wasm-unsafe-eval' https://*.posthog.com https://us-assets.i.posthog.com https://app.posthog.com;
    connect-src 'self' https://*.posthog.com https://us.i.posthog.com https://us-assets.i.posthog.com https://app.posthog.com;
    img-src 'self' data: https: http:;
    style-src 'self' 'unsafe-inline';
    font-src 'self';
    frame-src 'self' https://*.posthog.com;
  "> -->
  <Posthog />
  <ClientRouter />
</head>
